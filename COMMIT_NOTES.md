# 本轮变更说明（批次 16–24）

> 承接首轮「184 页逐页比对」之后的第二轮工作。
> 主线是 **D10 语音表翻译收尾**，过程中又发现并修复了 D11 / D12 两类新缺陷，最后补上 D13 跨字段校验体系。
> 详细逐批日志见 `TASKLIST.md` 的「第二轮批次日志」一节。

---

## 一句话摘要

**全站 72 张语音表翻译清零（约 1210 条台词）；修复 2 起「语音表张冠李戴」；补齐 5 名六原职员资料并订正 3 处实装日；新建跨字段交叉校验体系。测试 167 → 179。**

---

## 建议的 commit message

```
feat(voice): 全站 72 张语音表翻译清零，修复张冠李戴与六原资料缺失

- D10 收尾：8 个批次译完 32 张语音表约 1210 条台词，
  51 地魂 + 11 六原 + 10 妖怪全覆盖，断言升级为「未翻译数必须为 0」
- D11 修复：echizen 的语音表装的是播磨的台词、izumi 整表是伊豆的复制品，
  两页回原页重建；全站复查无重复表、无高重叠表
- D12 修复：5 名六原职员补齐年龄/生日，订正 3 处实装日、4 处所属，
  统一 soma 的中日混用键名
- D13 新增：跨字段交叉校验（8 维度），检查器经注入式自检确认有效
- 顺带修复 mikawa / shimotsuke / noto / yamashiro 四页的语音表结构错误

Tests: 167 → 179
```

---

## 变更明细

### 1. D10 · 语音表翻译（主线，已清零）

分 8 批完成，每批都回原页逐行比对后再翻译。

| 批次 | 角色 | 条数 |
| :-- | :-- | --: |
| 16 | `shima` `osumi` `sagami` | 127 |
| 17 | `omi` `wakasa` `mikawa` `tosa` | 169 |
| 18 | `tanba` `owari` `yamato` `shimotsuke` | 176 |
| 19 | `shimousa` `musashi` `shinano` `suruga` | 181 |
| 20 | `echizen` `izumi` `oki` | 132 |
| 21 | `yamashiro` `totomi` `suo` `nagato` | 167 |
| 22 | `sanuki` `noto` `mutsu` `tajima` | 181 |
| — | **合计**（含此前已完成的妖怪/六原 58 条） | **~1210** |

**翻译方针**（全程一致）：

- **方言不硬造中文方言**，靠语气词与用词的土气程度还原。津軽弁「じょっぱり」译作「倔脾气」、能登弁「あんやと」按语气还原、近江商人腔用「少爷／多谢惠顾」的商贾口吻。
- **专有名词一律保留不意译**：千两天秤、近淡海、御食国、建依别、一领具足、熊袭建、日本武尊、弥五郎大人、方违之术等。
- **不可译的当て字按本意翻译**：暴走族的「夜露死苦」「仏恥義理」译作「请多指教」「讲义气」并保留吼叫感——照抄汉字中文读者看不懂。
- **地方童谣保留原文**：山城分岐台词的京都街道数歌（丸竹夷二押御池、姉三六角蛸锦）是记路名用的，音译即失义。
- **原页没有的不编造**：`shimotsuke` 的「追击」「味方负伤」、`yamashiro` 的「发现宝藏」「味方负伤」原页本身为空，标注「（原页未记载）」。
- 全站统一「负伤(衣服破损)」写法，沿用既有译名「晴服」（对应原页「晴れ着」，21 页 59 处）。

### 2. D11 · 语音表张冠李戴（新发现，性质最严重）

翻译 `echizen` 时发现其语音表内容根本不是越前的。扩查确认两起：

| 角色 | 症状 |
| :-- | :-- |
| `echizen` 越前·三十九番·重装·北陆道 | 整表装的是**播磨**的台词（自称「僕は、山陽道、播州、播磨！」、犬系语尾、姬路城、赤穗四十七士） |
| `izumi` 和泉·六番·畿内 | 整表是 **`izu`（伊豆）的逐字复制品**，40 行一字不差 |

**`izumi` 此前一直显示为「已翻译」**——中文流畅、格式完整、假名占比为 0，所有既有校验全过。这是本站首次发现「假的已完成」。

两页均回原页重建。全站复查：完全相同的语音表 0 组、任意两角色台词重叠率 >30% 的 0 对、51 地魂「资料室台词含本人名字」全通过。

### 3. D12 · 六原职员资料（补齐既有遗留）

**关键：原 wiki 页面名用的是简称，不是全名。** 此前按 `犬童夏人` 抓返回「页面不存在」，误判为「没有独立页面」；实际是 `夏人`／`猪森`／`羽鳥`／`百太`／`虎鉄丸`，入口在 `ロクハラ` 母页。

| 角色 | 补齐年龄/生日 | 同时订正 |
| :-- | :-- | :-- |
| `natsuhito` | 21 ／ 5月19日 | 所属补「接线员勤务」 |
| `inomori` | 26 ／ 9月17日 | **实装日 v1.30 → v1.01**；所属补「研究室（Lab）勤务」 |
| `hatori` | **不详** ／ 2月20日 | **实装日 v1.30 → v1.01**；所属改「原大学教授　总司令官代理」 |
| `hyakuta` | 22 ／ 3月29日 | 所属改「文学作家的助手　影子写手」 |
| `kotetsumaru` | 19 ／ 6月9日 | **实装日 v1.53 → v1.522**；所属改「环境声闻课（兼职）」 |

- `hatori` 的年龄原页即写「不詳」，如实译出而非留空或编造（原页小ネタ提到职员常拿他年龄当话题，是设定本身）。
- 顺带统一 `soma` 的中日混用键名（全站唯一用「年齢／誕生日／实装」者）并译出所属。
- 9 名六原职员 kv 现统一为五项、键顺序一致。`riku`／`azukiarai` 原页确无属性表，测试显式豁免。

### 4. 语音表结构错误（4 页，与翻译独立）

这类错误**列数整齐、格式完整**，既有校验查不出：

| 角色 | 问题 |
| :-- | :-- |
| `mikawa` | 「水着」「おでかけ私服」在 3 处被塞进同一单元格，用 `<br>おでかけ私服<br>` 硬连 |
| `shimotsuke` | 同上 3 处；**「敌对峙」整组（原页独立 Enemy 表 6 行）被接在战斗表尾部**，导致多个场景重复出现；「走光」少一条 |
| `noto` | 「メイド服」3 处被塞进上一行；**「走光·女仆装」台词整条丢失** |
| `yamashiro` | 「拔魂技前」三行**整体错位**丢失一条；「湯浴み」被误写成「沐浴」与相邻场景撞名；「おーきに」误作「おおきに」 |

### 5. D13 · 跨字段交叉校验（新建体系）

新增 `scripts/cross_check.py`，8 个维度比对字段间一致性，全站 72 角色**零命中**：

立绘路径前缀=slug ／ 图片 URL 页面名=角色名 ／ 羁绊不含他人名 ／ 声优绘师名一字之差 ／ 资料室台词≈descZh ／ 地魂所属∈五畿七道 ／ num=numInt ／ 正文角色名用字=nameZh

**零命中本身可疑，故先做注入式自检**：往干净数据注入 5 类已知错误（立绘改成 `owari_0.jpg`、portrait 换尾张页、羁绊塞「尾張の心」、numInt 改 99、所属改「火星道」），确认每项都能抓到且原始数据不误报。5/5 有效。

「资料室台词≈descZh」判据经两次返工：字面相似度 → 72/72 全员误报（因 descZh 与台词是同一段日文的两次独立翻译）；专有名词抽取 → 仍误报 20 个；最终用**汉字集合重合度**，正常区间 0.62~1.00，模拟 D11 仅 0.15，阈值取 0.40。六原短问候语按长度跳过。

---

## 文件变更清单

**角色数据（32 个）**
```
src/data/pages/{shima,osumi,sagami,omi,wakasa,mikawa,tosa,tanba,owari,yamato,
  shimotsuke,shimousa,musashi,shinano,suruga,echizen,izumi,oki,yamashiro,
  totomi,suo,nagato,sanuki,noto,mutsu,tajima}.json      voiceMd 翻译/重建
src/data/pages/{natsuhito,inomori,hatori,hyakuta,kotetsumaru,soma}.json   kv 补齐订正
```

**测试（167 → 179）**
```
tests/data-integrity.test.ts   +12 条
  · D10 断言升级为「全站未翻译数必须为 0」
  · D11：无重复语音表 / 台词重叠率 ≤30% / 资料室台词含本人名字
  · D12：六原 kv 五项齐全 / 年龄生日有值无日文键名 / 实装日与原页逐字一致
  · D13：立绘前缀 / 图片URL / 羁绊 / num / 所属 / 资料室台词≈descZh
```

**脚本**
```
scripts/cross_check.py     新增（已加 .gitignore 白名单）
.gitignore                 +1 行白名单
TASKLIST.md                +第二轮批次日志、D11–D13 三节
```

---

## 已知遗留

- **薩摩语音只有 27 条**：原页个室 5 条、沐浴、限界突破等仍是编辑占位，等原站补全后同步。非本地缺失。
- **已知误报（非问题）**：内容零丢失校验器对 `daily.md`、`short.md` 索引页会报 Markdown 链接渲染成 `<a>` 后匹配不上。

## 过程中我自己造成并已修复的错误

如实记录，供日后避坑：

1. **`TASKLIST.md` 被截断**（批次 18）：用 Python `open(p,'w')` 追加日志，该模式先清空文件，随后 `write()` 抛 `UnicodeEncodeError`，导致文件从 59230 字节剩 1523 字节，首轮 15 批日志全丢。已从 GitHub 拉回基线重建。**教训：改 TASKLIST 一律用 heredoc 追加。**
2. **`sanuki` 译文写成简体「赞岐」**（批次 22，4 处）：与全站统一用字不符。**由批次 20 新加的 D11 测试「资料室台词必须含本人名字」当场报错抓到**，说明该测试有效。
3. **误判「六原职员无独立页面」**（批次 14 → 23 修正）：按全名检索失败就下了结论，实际页面用简称。

---

## 校验状态

```
vitest      179 passed (6 files)
vue-tsc     ✓ no errors
vite build  ✓ built
工作区      83MB / 1269 文件（不含 node_modules、dist）
.git        已删除
```
